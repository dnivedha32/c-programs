#include <stdio.h>

float calculateAverage(int a, int b, int c) {
    return (a + b + c) / 3.0;
}

int main() {
    int x, y, z;

    printf("Enter three numbers: ");
    scanf("%d %d %d", &x, &y, &z);

    printf("Aver
      
age = %.2f", calculateAverage(x, y, z));

    return 0;
}
output:
Enter three numbers: 10 20 30
Average = 20.00
