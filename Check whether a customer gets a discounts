#include <stdio.h>

int main() {
    float amount;

    printf("Enter purchase amount: ");
    scanf("%f", &amount);

    if (amount >= 2000)
        printf("Customer gets a discount\n");
    else
        printf("Customer does not get a discount\n");

    return 0;
}
output:
Enter purchase amount: 2500
Customer gets a discount
